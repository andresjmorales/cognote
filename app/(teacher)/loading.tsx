import { AppLoadingScreen } from "@/components/brand/AppLoadingScreen";

export default function TeacherLoading() {
  return <AppLoadingScreen message="Loading..." className="min-h-screen bg-background" />;
}
