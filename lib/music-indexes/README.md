# Music source indexes

Generated by `npm run build:music-indexes` from:

- OpenScore Lieder `data/scores.tsv` + git tree (`.mxl` paths on `main`)
- OpenScore String Quartets `data/scores.tsv` (+ MXL when present)
- Mutopia `musiccache.dat`

Commit these JSON files so search works offline for Mutopia/OpenScore. IMSLP remains a live MediaWiki API call.
